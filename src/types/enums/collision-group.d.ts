/**
 * ### Enum that represents different collision groups in the game.
 *
 * These groups are used to categorize objects based on their interaction with
 * other objects.
 */
export declare const enum CollisionGroup {
	Character = "Character",
}
